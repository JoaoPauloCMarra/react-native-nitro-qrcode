import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  frame: {
    overflow: "hidden",
  },
  image: {
    width: "100%",
    height: "100%",
  },
  logo: {
    alignItems: "center",
    justifyContent: "center",
    overflow: "hidden",
    pointerEvents: "none",
    position: "absolute",
  },
});
